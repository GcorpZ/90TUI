# HANDOFF — Sesión 2026-09-11 (cerrar el boliche)

> Leer antes de picar código. Rama de trabajo: `g90tui-built` → HEAD `797b9bb`.
> Orden permanente vigente: **después de cada cambio, commit + push** (salvo
> contraorden). `docs/CONTROLES.md` se mantiene al día fila por fila.

## 1. Dónde quedó cada cosa (GitHub: GcorpZ/G90TUI)

11 commits HOY sobre `g90tui-built` (base `033d9a9` = handoff de ayer):

| Commit | Contenido |
|---|---|
| `938ac3c` | `shadow`: `darken_color` neutro por luminancia Rec.601 (cian→gris oscuro, jamás `Navy`); showroom a tema único. Cierra el problema de sombras azules del HANDOFF-10 |
| `25b8e2c` | `icons20`: drives NF homogéneos (`f0a0`, 5 celdas, C: activa en amarillo), `fkey_badge` con superíndices F¹–F¹²; showroom alineado sobre navy |
| `6115b8b` | archivos estilo Norton sin glifos (`file_fg` por ext, nombres al ras); `win_close_framed` integrado al marco (`┤■├`/`╡■╞` en x+3) |
| `7f43bcc` | F-keys con vida: `F` amarilla + número `DarkYellow` (nuevo `Color`), control `FKeyBar` (`cant_funcs` + `funcs_loads`, `Overflow` si sobran); showroom la usa |
| `507c8d4` | showroom: **F3 sale, F10 entra al menú** (`menubar_key`: mueve/abre/cierra, restaura último) |
| `a856c13` | charts: `VirtualCanvas` braille 2×4 + Bresenham (`prim::canvas`, exportado en raíz); `Line` migra al motor |
| `fa573f4` | showroom: diálogo central 64→67 (+3 aire) |
| `f53b054` | showroom: diálogo 67→70 (+6 total). OJO: no ensanchar más en 80×25 (come paneles) |
| `ec08211` | charts pro: `Line` Navy+ejes `┤┼─┴`+badge último valor (amarillo anulado), `Pie` = barra 100% apilada 2 filas + leyenda `■ A (50%)`, barras sólidas con escala Y, velas OHLC (`Candle`/`candle_draw`) |
| `e824cb5` | botones con foco por luminancia: hotkey `&` + `focused` (Blue/bold) + `pressed` (Navy); fuera mancha negra (`msgbox`/`confirm`) y `►` externos de botones |
| `797b9bb` | tabs archivador CUA: orejetas `│ │` + marco CP437 con apertura, viewport estricto `(x+1,y+2,w-2,h-3)`; showroom con 3 páginas aisladas (General/Inventario/**Archivos**) en azul tarjeta |

Ramas `main`, `g90tui-docs`, `g90tui-help`: en sync con remoto, sin cambios hoy.
PRs pendientes (en este orden): docs → built → help → `main`.

## 2. Estado verificado (cierre)

* **195/195 tests** (178 lib + 5 core + 5 app + 3 prim + 4 widgets), clippy 0
  warnings (`-D warnings`), `cargo fmt` limpio, `cargo check --examples` OK.
* Árbol limpio sobre `797b9bb`, todo pusheado a `origin/g90tui-built`.
* `showroom` NO se ejecuta en este entorno (interactivo, cuelga) — validación
  por tests + `cargo check`. **Calibración visual pendiente del usuario**:
  F4 (contraste/ejes), tarjeta de pestañas, foco de botones, menú F10.

## 3. Decisiones cerradas (no re-discutir sin motivo)

* **Motor ANSI-16, sin `Rgb`**: los pedidos TrueColor se mapean
  (`shadow.rs`, `button.rs`, `color.rs` lo documentan). `DarkYellow` sí se
  agregó como variante (marrón VGA → `DarkYellow` ANSI).
* Sombras: solo escala neutra (Grey/DarkGrey/Black), jamás tinte.
* Drives NF: glifo único `f0a0`, 5 celdas; Retro intacto.
* `fkey_badge`: `F` Yellow + superíndice DarkYellow + acción blanca + 2 aire.
* `FKeyBar`: slots explícitos `(nº, acción)` (soporta huecos F9/F10),
  `load_many` atómico, `Overflow { cant_funcs, tried }`.
* F3 = salir, F10 = menú (con `menubar_key`; menús demo con `&[]`).
* Charts:   `Line` en Navy sobre blanco SIEMPRE (amarillo anulado por diseño);
  `Pie` conserva el nombre pero es barra apilada; `Candle` solo librería
  (F4 muestra 3 paneles, sin velas por ahora).
* Botones: foco = botón encendido (`Blue`/bold), `&` nunca se dibuja,
  `button()`/`button_draw()` conservan firma (reposo).
* Tabs: paleta CUA fija (inactiva tenue, activa blanco/azul); `Tab.fg/bg`
  quedan reservados; viewport Top estricto; scopes FM por página
  (General/Inventario/Archivos); progreso+botones comunes bajo la tarjeta.
* Diálogo showroom: 70×21 (tope en 80×25). Hijos en azul tarjeta vía
  `vp` + `tab_bg`; inputs/lista con fondo azul seteado en constructor.
* FileDialog sin navegación de botones → reposo (sin foco que mostrar).

## 4. Trampas aprendidas (no repetir)

* `.exe` zombies → LNK1104 (pasó hoy con showroom): matar antes de rebuild.
* `Color` nuevo = tocar `to_crossterm` + `rgb_approx` + arrays de tests.
* Campos nuevos en structs públicos rompen literales (`ButtonOpts{}` en tests).
* `parse_hotkey` ya resolvía `&` + 1ª letra: reutilizar, no reinventar.
* Funciones solo usadas en tests → `#[cfg(test)]` o clippy `dead_code` muerde.
* Asserts con offsets de celdas: contar el espacio interno (`│ Uno │`).
* Mensajes del usuario llegan cortados a veces: lo uni-punto se confirma,
  lo visual se pregunta antes (lección HANDOFF-10 vigente).

## 5. Mañana (en este orden)

1. `cargo run --example showroom` con JetBrainsMono NF: calibración visual
   (F4, tarjeta, botones, menú F10, sombras neutras).
2. Items reales en menús F10 y/o velas en F4 (a pedir del usuario).
3. PRs a `main`: docs → built → help (ese orden).
4. `g90tui 0.1.0` en crates.io + cursor en forms.
5. Roadmap README: mouse, temas serde, más widgets.
