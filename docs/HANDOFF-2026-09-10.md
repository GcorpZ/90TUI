# HANDOFF — Sesión 2026-09-10 (cerrar el boliche)

> Leer antes de picar código. Rama de trabajo: `g90tui-built` → HEAD `0eacc32`.
> Orden permanente vigente: **después de cada cambio, commit + push** (salvo
> contraorden). `docs/CONTROLES.md` se mantiene al día fila por fila.

## 1. Dónde quedó cada cosa (GitHub: GcorpZ/G90TUI)

13 commits HOY sobre `g90tui-built` (base `ec8224a` de ayer):

| Commit | Contenido |
|---|---|
| `85371d2` | rename total `90TUI/tui90` → `G90TUI/g90tui` (marca + crate, 23 ficheros) |
| `a452494` | `CalendarPicker`: campo contraído + popup, `open/accept/cancel`, `calendar_key_mod` (año con Shift/Ctrl), tab General en showroom |
| `f3cebde` | calendar: hoy en bold+rojo (`today_fg`), `datemask` opt (`YYYY/YY/MM/M/DD/D`), field width dinámico |
| `5ba1bc1` | calendar: popup con bordes de línea simple `┌─┐│└┘` |
| `1a3b37e` | `shadow`: `Translucent` cromático con `darken_color` (fg ×0.50, bg ×0.40) |
| `e0821c8` | `window`: `BorderStyle::{None,Single,Double,Bevel3D}`, presets con `Double`, título incrustado |
| `f1c96ef` | window: presets con `Single` por defecto (programador elige) |
| `a8ed4f3` | `progressbar`: relleno sub-celular 8x (octavos), brackets/caja, `progress_draw` reutiliza widget, showroom anima 0→100 en ciclo |
| `274a698` | `icons20` nuevo (dual RetroCp437/NerdFont: drives, fkey, wins, tree), showroom lo usa (`icons.rs` intacto) |
| `2da70d9` | showroom: `fkey_badge`, `win_zoom`+grip en diálogo, `file_badge` por extensión |
| `b8a4351` | icons20 correctivo: sin fondos hardcodeados (`bg` dinámico), file 2 celdas, fkey Norton |
| `1125dde` | icons20: fkey con prefijo `F`, drives carcasa PC Tools (`[▬-]` LED verde, letra blanca), árbol azul continuo (badge bg = azul real de la fila, NO `selected_bg`) |
| `0eacc32` | icons20: drives NF uniformes (`f0a0/f4bc/f111`); showroom desktop → cian + panel izq `Translucent` explícito |

Más el rename de ayer/hoy fuera de `built`: repo `90TUI` → `G90TUI` (web GitHub),
remoto actualizado a `https://github.com/GcorpZ/G90TUI.git`, ramas
`90tui-*` → `g90tui-built/docs/help` (local + remoto, viejas borradas).
`g90tui` verificado libre en crates.io (API 404). `gh` CLI no existe aquí.

## 2. Estado verificado (cierre)

* **178/178 tests** (161 lib + 5 core + 5 app + 3 prim + 4 widgets), clippy 0
  warnings (`-D warnings`), `cargo fmt` limpio, `cargo check --examples` OK.
* Árbol limpio sobre `0eacc32`, todo pusheado a `origin/g90tui-built`.
* `showroom` NO se ejecuta en este entorno (interactivo, cuelga) — validación
  por tests + `cargo check`.

## 3. ⚠️ PROBLEMA ABIERTO (no tocar nada más hoy, primera tarea mañana)

**El usuario ve las sombras AZULES y el showroom peor que antes. Tiene razón.**
Cadena de errores míos:

1. `darken_color` mapea por tinte (Cian→Teal→Navy): sobre fondos de color
   la sombra queda AZUL en vez de oscurecerse a neutro oscuro. Una sombra
   real debe ir hacia neutro oscuro, no conservar tinte.
2. Para "arreglar" el bloque negro bajo el panel (DarkGrey→Black por tabla),
   cambié el desktop del showroom a cian (`t.desktop`) → ahora TODAS las
   sombras caen a Navy. Peor.
3. Además encadené 3 rediseños de `fkey_badge` y 3 de drives en un día
   (superíndices → sin F → con F; carcasas → NF → carcasas → NF),
   sin validación visual entre ellos (no hay cómo ver el showroom aquí).

**Plan mañana (acordar con el usuario ANTES de picar):**
* a) Revertir desktop del showroom a `DarkGrey` (o el que elija) y
* b) Rediseñar `darken_color` hacia neutro oscuro (p.ej. cian→Teal→Black,
  o gris oscuro directo) en vez de conservar tinte, o
* c) Volver al `Translucent` original (bg negro + `dim`, glifo intacto) que
  era el look aprobado del HANDOFF-09.
* NO más rediseños en cadena sin visto bueno visual por paso.

## 4. Decisiones cerradas (no re-discutir sin motivo)

* Marca `G90TUI` / crate `g90tui` (minúsculas obligatorias; `90tui` es
  identificador Rust inválido). Memoria del rename en HANDOFF-08:25.
* `CalendarPicker` cerrado por hoy: campo + popup línea simple + `datemask`
  + hoy bold+rojo + tab General con foco `fecha` (13º) y modificadores.
* `BorderStyle` en `window.rs`, presets con `Single`; título incrustado en
  marco, `[■]` en `x+1..x+3`, sombra `Translucent` (2,1), cuerpo primero.
* `ProgressBar` 8x con octavos + frontera no pisada por `NN%`; `progress_draw`
  usa el widget; showroom anima 0→100 (`tick_progress` 150ms).
* `icons20` convive con `icons` (intacto); `win_*` solo por ruta completa
  (`prim::icons20::`) para no chocar. Firmas con `bg` dinámico en
  `folder_badge`/`file_badge` (jamás hardcodear fondos).
* File badge = 2 celdas (glifo + aire), nombre en `x+2`.
* F-keys estilo final: `F1 Help` (F amarilla bold, acción blanca, navy, 2 aire).
* Tree: badge con el bg REAL de la fila (azul `popup` en selección — a
  propósito NO `selected_bg` gris, que era el parche).
* Botón conserva su sombra propia `▄/▀` (sistema aparte, pendiente unificar).

## 5. Trampas aprendidas (no repetir)

* `.exe` zombies → LNK1104: `Get-Process showroom,... | Stop-Process` antes.
* En pwsh no hay `rg`/`head`/`gh`: `Select-Object`, grep-tool, web para GitHub.
* `cargo fmt` refluye y rompe `oldString`: releer antes de cada edit.
* `replaceAll` en cadena (`TUI90`→`G90TUI` y luego `90TUI`→`G90TUI`) duplica
  prefijos (`GG90TUI`): ordenar reemplazos o verificar con grep después.
* `clippy::too_many_arguments` en firmas de contrato: `#[allow]` justificado.
* `calendar_key`/`Show::key` ahora llevan modificadores (`KeyModifiers`).
* `Rect` + `saturating_*` siempre; `buf.set` recorta silencioso (no paniquea).
* No reescribir HANDOFFs viejos (historia); solo el nuevo.

## 6. Mañana (en este orden)

1. Fijar sombras con el usuario (§3: revert desktop + darken neutro u original).
2. `cargo run --example showroom` con JetBrainsMono NF: calibración visual real.
3. PRs a `main`: docs → built → help (ramas ya renombradas a `g90tui-*`).
4. `g90tui 0.1.0` en crates.io + cursor en forms.
